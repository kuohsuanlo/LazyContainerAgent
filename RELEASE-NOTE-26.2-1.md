# LazyContainerAgent v26.2-1

> SHA-256(`LazyContainerAgent.jar`):`1850a9c805d53107c695fc1463ecdb73611af8f89a9283f2ec7462379e2739b7`

## 這版改了什麼

**修復一個邊角掉物/複製漏洞。** 容器(箱子/木桶/界伏盒)存檔時,原本會把「暫存的原始物品資料」本體直接寫進存檔結果;這版改成寫一份複製品。

會踩到這個洞需要同時滿足:容器**沒被玩家開過**、而且有人用 `/clone` 複製它或直接用 `/data` 指令改它的存檔資料。一般玩家日常操作不會碰到,但一旦踩到,後果是物品消失或被複製——所以修了。修法只加一行(`raw.copy()`),對效能幾乎無感,已在真實 Paper 26.2 環境驗證開機正常、零錯誤。

## 🧠 這版經過 Claude Fable 5 的對抗審計

用 49 個獨立 AI agent 分工:一半負責從各種角度挑毛病(讀取路徑、記憶體、`/clone`/`/data`/結構方塊等邊角操作、bytecode 注入正確性),另一半負責用「預設你是錯的」的立場反過來推翻這些發現。

**結果:14 條發現全數撐過反駁、0 條被推翻**——確認這個外掛:
- **不會亂吃記憶體**(暫存資料生命週期封閉、三條路徑保證釋放,無洩漏)
- **不會白做工**(存檔是淨省,不是重複勞動;跳過重新打包才是它存在的意義)
- **正常玩家操作零掉物風險**(窮舉容器讀寫、破壞掉落、漏斗、比較器、雙箱、戰利品箱、`/clone`、結構方塊、`/data` 指令,無一條會多丟東西)

順便還查清了一次「這外掛被冤枉」的事件:某分流一次主線程卡頓的自動診斷把責任怪到這外掛頭上,審計逐行核對原始碼後判定**不成立**——真兇是某件物品身上被寫入的異常巨大自訂資料,原版存檔邏輯處理它本來就會卡,跟這外掛無關;拔掉外掛不會解決,反而更糟。

完整報告:[`FABLE5-AUDIT.md`](FABLE5-AUDIT.md)。

## 文件

順手把幾份技術文件(`FINDINGS.md`、`ADVERSARIAL-REVIEW.md`、`TESTING.md`、README 中英文版)裡對非 NMS 專家不好懂的行話補上白話說明,數字/檔名/程式碼一律沒動,純粹是讓人看得懂在講什麼。

## 升級方式

跟平常一樣:換 jar、停服重啟。**不要用熱重載**(這是 Java agent,`-javaagent:` 開機掛載,熱重載換不掉)。硬碟存檔格式完全沒變,不需要任何資料遷移;真要回滾,拔掉 `-javaagent:LazyContainerAgent.jar` 那幾行參數重啟就回到裝之前的狀態。

---
Crafted by 廢土貓大 LogoCat · [mcfallout.net](https://mcfallout.net)
