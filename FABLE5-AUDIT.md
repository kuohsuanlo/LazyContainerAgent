# LazyContainerAgent — Fable 5 對抗審計報告(2026-07-02)

> 本檔記錄一輪由 **Claude Fable 5**(Anthropic 目前最高階的正式模型)主導、49 個獨立 agent 分工執行的**對抗式審計**(adversarial audit):找碴的人和裁決的人不是同一批,裁決者的預設立場是「先假設你錯,拿證據來說服我」。目的是回答三個最樸素的問題——**這東西會不會亂吃記憶體?會不會白做工、浪費硬碟?會不會弄丟玩家的東西?**
>
> 這份報告與既有的 [`ADVERSARIAL-REVIEW.md`](ADVERSARIAL-REVIEW.md)(12-agent、8 種失效模式,較早期的一輪)是**互補關係**,不是取代:本輪規模更大(6 個獨立找碴方向 × 每條發現 3 種角度反駁 + 1 個「你們是不是漏掉什麼」的總批評),而且是針對**目前這版程式碼**(含本檔案記錄的修復)重新驗證的。

## 一句話結論

**這外掛本體是健康的**:記憶體有界不洩漏、存檔是淨省不是白工、正常玩家操作零掉物風險。審計揪出一個**真實但要靠管理員指令或外掛直接改存檔資料才會踩到**的邊角漏洞(下面「已修」那節),已經修了一行、實機驗證過。另外也把一次「這外掛被冤枉」的誤判事件(s109 2026-07-02 主線程卡頓)查清楚,還了它清白。

---

## 審計怎麼做的

分三階段,全程要求每個判斷都要引用 `檔名:行號`,不准憑印象腦補:

1. **找碴(Find)** —— 6 個各自獨立的 agent,分頭窮舉不同角度:誰有可能繞過「延遲解碼」的守門機制而讀到空容器、存檔/載入的 NBT(Minecraft 存資料的二進位格式)路徑型別是否真的每處都符合假設、記憶體會不會滯留、`/clone`、`/data`、結構方塊這些邊角指令會不會弄丟東西、以及 bytecode 注入(用 ASM 這個工具在 JVM 啟動時把外掛的邏輯「嫁接」進 Minecraft 伺服器本體的程式碼裡)本身有沒有寫錯。每個 agent 都對照 **Paper 26.2(對應 Minecraft 1.21.11)的官方原始碼反編譯版**逐行核對,不是憑感覺猜。
2. **反駁(Verify)** —— 每一條「找碴」找到的問題,交給 3 個互不知情的裁決者,分別從「原始碼真的長這樣嗎」「能不能具體構造出一個會踩到這問題的情境」「就算為真,傷害真的有那麼嚴重嗎」三個角度嘗試推翻它。裁決者的預設立場是懷疑、不是相信;一條發現要撐過 3 個裁決者裡至少 2 個都說「推不翻」才算數。
3. **總批評(Critic)** —— 最後一個 agent 專門檢討「這輪審計本身漏掉了什麼」,列出沒查到、或礙於沒有原始碼可查而只能存疑的部分,誠實承認證據鏈的邊界在哪。

**成績**:14 條發現撐過反駁存活、**0 條**被推翻;另外窮舉查驗後確認安全的面向有 84 條。嚴重度分布:**0 個 critical**、2 個 high(其中 1 個是幫「s109 卡頓」的事件歸因平反,不是程式漏洞;另 1 個只在「動態掛載」這種你們目前沒在用的部署方式下才成立)、5 個 medium、5 個 low、1 個 info。

---

## 回答最初的三個問題

| 問題 | 結論 |
|---|---|
| **會不會亂吃記憶體?** | 不會。暫存的原始資料(`raw`)生命週期封閉、三條路徑(容器被存取物化 / 容器被整批換掉 / 區塊卸載)都會釋放,沒有滾雪球式洩漏。代價是:一般物品的暫存資料大約是「原版直接解碼成物件」的 2–4 倍大小(有明確上限,對含超大自訂資料的物品反而更省)。 |
| **會不會白做工、浪費硬碟?** | 不會,是淨省。核心設計就是「沒被碰過的容器,存檔時原封不動把讀到的原始位元組寫回去,整段重新編碼全部跳過」;存出來的檔案格式、大小跟原版完全一樣。實測 13 台正式機的計數器全部 `eagerLoad=0`(代表「應該省的都真的省到了」)。唯一會讓它變慢、甚至比原版更慢的,是特地開啟的「shadow 驗證模式」——這模式本來就只該在上線前跑一段時間驗證用,不該長期開著。 |
| **會不會弄丟玩家東西?** | 正常玩遊戲不會。窮舉了容器載入/存檔、破壞掉落、漏斗、紅石比較器、雙格箱子、戰利品箱、`/clone`、結構方塊、`/data` 指令這些路徑後,**沒有找到任何一條「比原版多丟一件物品」的通路**。剩下的殘餘風險集中在一個要「管理員操作 + 沒被玩家碰過的容器」才會觸發的邊角漏洞——已經修了(見下)。 |

---

## 找到並已修復:raw 資料跨容器共用的邊角漏洞

五條發現(角度不同、但根因相同)指向同一件事:容器沒被碰過時,存檔會把「暫存的原始資料」**本體**直接塞進存檔結果,而不是塞一份「複本」。平常沒事,但下面兩種情況會共用到同一份資料、彼此互相污染:

1. **`/clone` 複製一個沒被開過的容器** → 複製出來的兩個容器會共用同一份物品清單,之後對其中一個下 `/data modify` 或 `/data remove` 改內容,**另一個容器會跟著變**(物品消失或被複製)。
2. **存檔正在背景寫入硬碟的那零點幾秒之內,剛好有管理員對同一個容器下 `/data` 指令** → 兩邊同時讀寫同一份資料結構,可能寫出撕裂或損壞的存檔。同族的風險也存在於**結構方塊(structure block)**擷取容器內容的路徑。

**這不是玩家日常操作會踩到的**(必須用到 OP 權限指令或外掛直接改存檔 NBT),所以先前沒有造成過線上事故。但它是個真實存在的洞。

**修法**(`template/io/github/kuohsuanlo/lazycontainer/LazyContainerTemplate.java:165`):

```diff
- out.put("Items", raw);
+ out.put("Items", raw.copy());
```

把「塞本體」改成「塞一份複製品」,容器自己留正本、存檔拿到的是獨立的複本,上述兩種情況從此互不干擾。複製一棵資料樹的成本遠比重新編碼便宜,對正常運作**效能幾乎無感**。

**驗證**:反編譯改完後的 class 檔,確認 `Tag.copy()` 這個複製呼叫確實編進了寫入 `Items` 之前的位置;並在真實 Paper 26.2 環境開機測試,`spliced 2 fields + 7 methods`、三個容器類別(箱子/木桶/界伏盒)全部改寫成功,**零** `VerifyError`/`NoSuchMethodError`/`NoSuchFieldError`,開機正常完成。

---

## 已知但暫不處理的項目

以下發現真實存在,但在**你們目前的部署方式**下**條件不成立**,或屬於「觀測性缺口」(不影響資料正確性,只是少了點除錯資訊),暫不需要動作:

- **shadow 驗證模式專屬的兩個問題**(生產環境沒開 shadow,不受影響):(a) 一致性比對不一致時,錯誤訊息沒有次數上限,長期開著可能洗版;(b) 比對用的字串在物品資料很大時會建出巨大的暫存字串,拖慢驗證速度。**結論:炸彈物品若要清理、且要重開 shadow 驗證前,先處理這兩點。**
- **動態掛載(`agentmain`,運行中途用工具重新附加 agent)專屬的風險**:若目標類別的欄位新增被 JVM 拒絕,會出現「宣稱注入成功但其實沒有」的狀態,導致容器存取時直接報錯。**你們的正式部署一律用開機掛載(`premain`,`-javaagent:` 開機參數),完全不受影響**,只有動態附加才會踩到。
- **`ensure`(容器首次被存取時的解碼過程)在物品資料損毀時完全不留紀錄**,原版至少會在載入時記一筆警告。這只是少了診斷線索,物品內容的處理結果跟原版完全一致,不是掉資料。
- **一般物品的暫存資料約為解碼後物件的 2–4 倍大小**——這是有界的正常代價,不是洩漏,寫進報告是為了更正舊版 `FINDINGS.md` 裡「暫存資料一定更省」這個過於樂觀的說法。

---

## 這輪審計自己承認的邊界(留給下一輪)

- **CraftBukkit 的「容器快照」(`getState()`)路徑沒有查完**——這是外掛最常用來讀取容器內容的管道之一,理論上跟已修的漏洞屬於同一家族,但因為對應原始碼不在本機可查的參考樹裡,只能存疑,沒有實際驗證。
- **記憶體 2–4 倍的估算是理論推算,不是拿真實伺服器實測出來的數字**(尚未用 `jmap`/`jcmd` 之類工具在正式機上量過實際 heap 占用)。
- **關掉 shadow 模式後,實際存檔幀時間到底省了多少 CPU,目前只有「快路徑計數器有在動」的間接證據,沒有拿掉外掛/裝上外掛的正式 A/B 時間對照**。

這些不影響「現在能不能放心用」的結論,但如果之後要做更深一層的效能驗證或處理外掛相容性問題,這是該優先補的地方。

---

## 版本與驗證紀錄

- 對應 `pom.xml` 版本:**`26.2-1`**。
- 建置環境:對真實 **Paper 26.2 mojmap(Minecraft 1.21.11)原始碼**重新編譯 `template/`,`JAVA_HOME` 用 JDK 25。
- 實機 smoke test:`spliced 2 fields + 7 methods into BaseContainerBlockEntity`,`ChestBlockEntity`/`BarrelBlockEntity`/`ShulkerBoxBlockEntity` 三個全部 `transformed`,開機正常完成,**零** `VerifyError`/`NoSuchMethodError`/`NoSuchFieldError`/`IllegalAccessError`。
- 正式機艦隊(13 台)線上計數器巡檢:`eagerLoad=0` 全數命中(代表「應該走快路徑的都真的走了快路徑」)、`shadowMismatch=0`、`benignReorder=0`,全數健康。

延伸閱讀:[`FINDINGS.md`](FINDINGS.md)(原始設計與反編譯事實)· [`ADVERSARIAL-REVIEW.md`](ADVERSARIAL-REVIEW.md)(較早一輪、12-agent 的對抗審查)· [`TESTING.md`](TESTING.md)(怎麼自己測)。
